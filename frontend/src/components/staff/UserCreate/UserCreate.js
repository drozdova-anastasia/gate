import './UserCreate.css';
import Header from '../../shared/Header/Header';

function UserCreate ({}) {
  return (
    <main className='user'>
      <Header/>
    </main>
  );
}

export default UserCreate;